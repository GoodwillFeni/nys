CONFIG_BLINK_LED_STRIP=y

# ── BLE (NimBLE) — ESP32-C3 is BLE-only ─────────────────────────────────────
CONFIG_BT_ENABLED=y
CONFIG_BT_NIMBLE_ENABLED=y
CONFIG_BT_CONTROLLER_ENABLED=y

# WiFi + BLE coexistence
CONFIG_SW_COEXIST_ENABLE=y
